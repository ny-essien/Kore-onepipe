# Requirements for Kore-onepipe
# Pin versions where known from project context (Django 5.2.x used).

Django
djangorestframework
djangorestframework-simplejwt
python-dotenv==1.0.0
requests==2.31.0
cryptography==41.0.4
pycryptodome==3.18.0
django-cors-headers


# Dev / testing
pytest==7.4.0
coverage==6.5.0

# Note: adjust versions to match your environment if needed.
